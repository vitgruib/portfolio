# Optional: override canonical URL for metadata (Open Graph). Leave unset on Vercel
# to use the automatic VERCEL_URL (your *.vercel.app deployment). Set this if you use
# a custom domain and want OG/canonical URLs to use it, e.g.:
# NEXT_PUBLIC_SITE_URL=https://yourdomain.com
